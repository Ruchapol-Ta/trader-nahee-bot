# CLAUDE.md

Read `AGENTS.md` first.

`AGENTS.md` is the source of truth for this repository.

If instructions conflict:
`AGENTS.md` takes precedence.

Follow all project rules, safety rules, runtime commands, validation commands, and current phase priorities defined in `AGENTS.md`.
